// Development environment (used by default with ng serve)
export const environment = {
  production: false,
  baseApiUrl: 'https://localhost:7048'
};

// For backwards compatibility
export const baseApiUrl = environment.baseApiUrl;